<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <h3><%= task.completed ? "[X]" : "[ ]" %><%= link_to task.title, task_path(task.id) %></h3>
<% end %>



<%= link_to "Add a new task!", tasks_new_path %>

